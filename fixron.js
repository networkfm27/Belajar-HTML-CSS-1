document
  .getElementById("contact-form")
  .addEventListener("submit", function (e) {
    e.preventDefault();

    // Lakukan sesuatu dengan data formulir, seperti mengirim ke server atau menampilkan pesan sukses.
    alert("Pesan telah dikirim!");
  });
